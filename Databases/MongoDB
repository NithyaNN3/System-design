### Partial matching 
> use /<word>/ as a value. Example: 

```ruby
// Query
db.users.find({ name: /like/ })

// Result
;[
  {
    email: 'one@email.com',
    name: 'One for like and comment',
  },
]
```
